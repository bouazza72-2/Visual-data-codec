/**
 * @file visual_tx.c
 * @brief Visual Data Codec (VCDC) - Implementation Translation Unit
 *
 * For traditional compilation environments (CMake, Makefile, Autotools, MSVC, Clang, GCC),
 * compile this file together with your application and include "visual_tx.h" in your source files.
 *
 * Alternatively, for header-only usage, omit this file and define VISUAL_TX_IMPLEMENTATION
 * in a single source file before including visual_tx.h.
 */

#define VISUAL_TX_IMPLEMENTATION
#include "visual_tx.h"
